How disc brake calipers mount to the frame or fork — commonly post mount (with max rotor size) or flat mount. Caliper, adapter, and mount type must agree.

Published as a categorical string (e.g. "post mount 180mm") because rotor size limits are part of the standard, not a separate numeric field.
